<?xml version="1.0" encoding= "UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="project">
	<select id="getLastno" resultType="int">
	<![CDATA[	
		select LAST_INSERT_ID()
	]]>  
	</select>
	
    <insert id="saveProject" parameterType="com.nbreds.projectPlanning.Project.VO.Project">
    <![CDATA[
		 insert into Project (
		  pname
		  ,pdetail
		  ,pdata
		  ,pduedate
		  ,pmember
		  ,uno
		) VALUES (
		  #{pname}
		  ,#{pdetail}
		  ,#{pdata}
		  ,#{pduedate}
		  ,#{pmember}
		  ,#{uno}
		)
    ]]>  
    </insert>
    
    <insert id="saveProjectMS" parameterType="com.nbreds.projectPlanning.Project.VO.ProjectMemberStat">
    <![CDATA[
		 insert into ProjectMemberStat (
		  uno
		  ,pno
		  ,stat
		) VALUES (
		  #{uno}
		  ,#{pno}
		  ,#{stat}
		)
    ]]>  
    </insert>
    
    <select id="getCodeTable" parameterType="String" resultType="com.nbreds.projectPlanning.common.VO.CodeTable">
    <![CDATA[
     select CODE_TYPE, CODE, CODE_NAME from CodeTable where CODE_TYPE=#{CODE_TYPE}
    ]]>  
    </select>
    
    <select id="getUsersForName" parameterType="String" resultType="com.nbreds.projectPlanning.common.VO.User">
    <![CDATA[
    select uno, uname, uphoneno, udepartment, uemail, uposition
    from User
    where uname like '%' #{uname} '%'
    ]]>  
    </select>
    
    <select id="getUserForNo" parameterType="int" resultType="com.nbreds.projectPlanning.common.VO.User">
    <![CDATA[
    select uno, uname, uphoneno, udepartment, uemail, uposition
    from User
    where uno=#{uno}
    ]]>  
    </select>
    
    <!-- 03-22 추가.. 모든 유저의 이름과 유저번호 가져옴 -->
    <select id="getAllUserNameAndNo" resultType="com.nbreds.projectPlanning.common.VO.User">
    <![CDATA[
    select uno, uname, uphoneno, udepartment, uemail, uposition
    from User
    ]]>  
    </select>
    
    <select id="getPageList" parameterType="hashmap" resultType="com.nbreds.projectPlanning.Project.VO.Project">
    <![CDATA[
		SELECT pno, pname, pdetail, pdata, pduedate, pmember, uno
		FROM Project
	]]>
		<where>
			<foreach collection="pdata" item="data" index="index" separator="or">
	<![CDATA[
				pdata like '%' #{data} '%'
	]]>
			</foreach>
		</where>
	
	<![CDATA[
		ORDER BY pno DESC
		LIMIT #{pageNo}, #{rowsPerPage};
	]]>
    </select>
    
    <select id="getCodeName" parameterType="hashmap" resultType="com.nbreds.projectPlanning.common.VO.CodeTable">
    <![CDATA[
      select CODE_TYPE, CODE, CODE_NAME from CodeTable where CODE_TYPE=#{CODE_TYPE} and CODE=#{CODE}
    ]]>  
    </select>
    
    <select id="getTotalProjectNo" resultType="int">
    <![CDATA[
   		select count(*) from Project;
    ]]>  
    </select>

    <select id="getProjectList" parameterType="hashmap" resultType="com.nbreds.projectPlanning.Project.VO.Project">
    <![CDATA[
	   	SELECT pno, pname, pdetail, pdata, pduedate, pmember, uno
		FROM Project
		ORDER BY pno DESC
		LIMIT #{pageNo}, #{rowsPerPage};
    ]]>  
    </select>
    
     <select id="getProjectByPno" parameterType="int" resultType="com.nbreds.projectPlanning.Project.VO.Project">
    <![CDATA[
     select pno, pname, pdetail, pdata, pduedate, pmember, uno from Project where pno=#{pno};
    ]]>  
    </select>
    
    <delete id="removeProject" parameterType="int">
    <![CDATA[
    delete from Project where pno = #{pno};
    ]]>  
    </delete>
    
    <update id="updateProject" parameterType="com.nbreds.projectPlanning.Project.VO.Project">
    <![CDATA[
    UPDATE Project SET
	    pname = #{pname},
		pdetail = #{pdetail},
		pdata=#{pdata},
		pduedate=#{pduedate},
		pmember = #{pmember}
	WHERE pno = #{pno}
	]]> 
    </update>
    
    <select id="getProjectByUno" parameterType="String" resultType="com.nbreds.projectPlanning.Project.VO.Project">
    <![CDATA[
    SELECT P.pno, P.pname, P.pdetail, P.uno
	FROM ProjectMemberStat S
	INNER JOIN Project P
	ON S.pno = P.pno
	WHERE S.stat=000 AND P.uno=#{uno} OR S.uno=#{uno}
    ]]>  
    </select>
    
    <!-- 페이징 처리를 위한 allProjectList -->
    <select id="allProjectList" parameterType="Map" resultType="com.nbreds.projectPlanning.Project.VO.Project">
    <![CDATA[
    SELECT pno, pname, pdetail, pdata, pduedate, pmember, uno
    FROM Project
    ]]>
    <if test="pdata != null">
		<where>
			<foreach collection="pdata" item="data" index="index" separator="or">
		<![CDATA[
				pdata like '%' #{data} '%'
		]]>
			</foreach>
		</where>
	</if>
	<![CDATA[
	ORDER BY pno DESC
	LIMIT #{start}, #{interval}
    ]]>
    </select>
    
    <!-- 페이징 처리를 위한 totalCount -->
    <select id="totalCount" parameterType="Map" resultType="int">
    <![CDATA[
    SELECT COUNT(*) cnt
    FROM Project
    ]]>
    <if test="pdata != null">
		<where>
			<foreach collection="pdata" item="data" index="index" separator="or">
		<![CDATA[
				pdata like '%' #{data} '%'
		]]>
			</foreach>
		</where>
	</if>
    </select>
    
    <select id="getCountRequestProjects" parameterType="String" resultType="int">
    <![CDATA[
	   	select COUNT(pno) FROM ProjectMemberStat where uno=#{uno} and stat='000'
    ]]>  
    </select>
    
    <select id="getRequestProjects" parameterType="String" resultType="com.nbreds.projectPlanning.Project.VO.Project">
    <![CDATA[
    select P.pno, pname, pdetail, pdata, pduedate, pmember, P.uno
	FROM Project P, ProjectMemberStat S
	WHERE P.pno = S.pno AND S.stat='000' AND S.uno=#{uno}
    ]]>  
    </select>
</mapper>